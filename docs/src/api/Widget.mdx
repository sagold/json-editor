import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import { DocsHeader } from '../components/DocsHeader';

<Meta title="api/Widget" />

<DocsHeader title="Widget" breadCrumbs="API Reference"></DocsHeader>

`Widget` selects and renders a widget for a given JsonNode

```tsx
import { useEditor, Widget } from '@sagold/react-json-editor';

function WebFormComponent() {
  const editor = useEditor(options);
  const node = editor.getNode();

  return <Widget node={node} editor={editor} options={options} />;
}
```

## Parameters

### `node: JsonNode`

### `editor: Editor`

### `options: JsonNode["options"]`
