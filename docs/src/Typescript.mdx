import { Meta, Canvas } from '@storybook/addon-docs/blocks';

<Meta title="Typescript" />

## Typed `UseEditor` Hook

<div className="code code--inline">

```ts
import { useEditor, JsonSchema, Widget } from '@sagold/react-json-editor';

type MyData = {
  title: string
};

function MyForm({ schema }: { schema: JsonSchema }) {
  // JsonEditor<MyData>
  const editor = useEditor<MyData>({
    schema,
    // (data: MyData) => void
    onChange(data) {}
  });
  return <Widget editor={editor} node={editor?.getNode()} />;
}
```

</div>

## Typed `JsonForm` component

<div className="code code--inline">

```ts
import { Editor } from '@sagold/react-json-editor';
import { JsonForm } from '@sagold/rje-mantine-widgets';

type MyData = {
  title: string
};

function MyForm({ schema }: { schema: JsonSchema }) {
  const [editor, setEditor] = useState<Editor<MyData>>();
  // data: MyData
  const data = editor?.getData();
  return (
    <JsonForm<MyData>
      // (editor: JsonEditor<MyData>) => void)
      editor={setEditor}
      // (data: MyData) => void
      onChange={(data) => {}}
      schema={schema}
    />
  );
}
```

</div>
