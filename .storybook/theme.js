import { create } from '@storybook/theming';

export default create({
    base: 'light',
    brandTitle: 'Json Editor Docs',
    // brandUrl: 'https://github.com/sagold/json-editor',
    brandTarget: '_self'
});
