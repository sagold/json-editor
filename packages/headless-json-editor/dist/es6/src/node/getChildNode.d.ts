import { Node } from '../types';
/**
 * returns child item or property if found
 */
export declare function getChildNode(node: Node, property: string): Node | undefined;
export declare function getChildNodeIndex(node: Node, property: string): number;
